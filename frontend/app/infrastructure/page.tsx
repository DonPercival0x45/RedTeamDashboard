// v2.0.0 nav route stub. Real infrastructure page (live VM inventory,
// Start/Stop, agent registry, CLI console) lands after Analytics.

import { PlaceholderPage } from "@/components/placeholder-page";

export default function InfrastructurePage() {
  return (
    <PlaceholderPage
      title="Infrastructure"
      tagline="Coming Soon....."
      detail="Live compute inventory and agent management are on the roadmap."
    />
  );
}
